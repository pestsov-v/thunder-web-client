import { ReactElement } from 'react';

export type SidebarWidgetProps = {
  className?: string;
};

export const SidebarWidget = (props: any): ReactElement => {
  return <div style={{ backgroundColor: 'red' }}>dsaddad</div>;
};
